rootProject.name = "b-plus-tree"

pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        jcenter()
        maven(url="https://dl.bintray.com/gradle/gradle-plugins")
    }
}